---
"create-cartwright": patch
---

Onboarding: the CLI now headlines the GitHub → Vercel path in its next-steps and offers an optional "Publish to GitHub now?" step (uses `gh`, fail-soft to manual instructions). Adds a `--no-github` flag (skipped under `--yes`). Links the new beginner guide at cartwright.app/docs/getting-started/from-code-to-live.
