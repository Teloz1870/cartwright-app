---
"create-cartwright": patch
---

npm README overhaul: correct quickstart (the CLI already installs, creates the schema and
seeds — the published README wrongly instructed `prisma migrate deploy` + `db seed`),
badges, live-demo links, and richer npm keywords (shopify-alternative, agentic-commerce,
mcp-server, create-next-app, storefront).
