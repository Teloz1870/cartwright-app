---
"create-cartwright": patch
---

Publish `create-cartwright` with npm build provenance (a SLSA attestation generated by the release workflow through GitHub OIDC). The package page on npm now shows that the published tarball was built from this repository by that workflow, and `npm audit signatures` can verify it. Nothing changes in the CLI itself. Tashan's independent index (2026-09-13) listed "no build provenance" as the package's primary weakness.
