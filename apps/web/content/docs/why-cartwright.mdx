---
title: Why Cartwright
description: How Cartwright compares to Shopify, Medusa, Saleor, and next-forge — and when not to use it.
---

Cartwright exists for websites and shops that want a full codebase, not a platform account.

## The short version

**Choose Cartwright when:** you want a real website or webshop in your own repo (one command
scaffolds a designed homepage — and, in the default profile, the database, admin and checkout); an AI agent should build and
operate it (MCP server, scoped tool registry and agent rules ship in the box); agentic-commerce
protocols (ACP, A2A, UCP) should be implemented, not promised; and you want no platform fee or
lock-in.

**Prefer something else when:** zero-ops hosted is the point ([Shopify](/compare/shopify),
[Wix](/compare/wix)); you want browser-only prompt-to-app with no terminal
([Lovable](/compare/lovable), [bolt.new](/compare/bolt-new), [v0](/compare/v0)); you need
enterprise multi-channel B2B ([Saleor](/compare/saleor)); or you're building something that
is not a website — a dashboard, an internal tool, a novel app ([create-next-app](/compare/create-next-app)).
For a plain website or a single page, `--profile site` is Cartwright's door — and for a one-off page
you will never grow, create-next-app is as fast; [Build a plain website](/docs/getting-started/plain-website)
says so too.

Shopify is the right default if you want managed commerce and accept SaaS constraints. You get a mature admin, hosting, payments, apps, and support. You also accept lock-in, platform rules, app-chain complexity, and limited control over the core runtime. Cartwright flips that tradeoff: you own the app and the source — and, in the default profile, the database, checkout flow and admin.

Medusa and Saleor are commerce engines. They are strong choices when you want a dedicated backend and are happy to bring or build the frontend. Cartwright's default profile is a complete shop template. It includes the storefront, admin, data model, checkout integration, AI assistant, MCP endpoint, and deployment recipe in the same Next.js app.

next-forge is a SaaS starter. It solves a different shape of problem: accounts, billing, emails, and application scaffolding. Cartwright takes the same "opinionated spine" idea and applies it to commerce: products, categories, carts, orders, Stripe checkout, admin media uploads, and commerce-specific AI workflows.

The main tradeoff is maintenance. Cartwright gives you code ownership, but ownership includes upgrades, bug fixes, integration drift, migrations, observability, and security. There is no commerce-specific support contract baked into the template. Community and paid setup help exist, but this is not a managed platform. When you outgrow the self-hosted setup, [Plus and Cloud tiers](/pricing) layer on support, Pro tooling, and (in Cloud) managed Vercel + Turso without breaking the open-source spine — with premium MCP integrations on the Plus roadmap.

## Migrating from Shopify or WooCommerce

Switching shouldn't mean rebuilding from scratch. **Hoptify** — the engine's tongue-in-cheek Shopify pendant — ships a parody "import from Shopify" onboarding (`/admin/hoptify`) that, with a Firecrawl key, genuinely brings your brand palette (via design-import) and products from the product URLs you list across, fail-soft to a demo otherwise. It needs `--profile full` — the module is pruned from the default profile — and pages, variants and SKUs are by hand today. It's a self-hosted on-ramp; the full agentic five-step migration (customers, deploy) is planned for [Plus](/pricing) and still in development.

Coming from **WooCommerce**, the parity toolkit covers the everyday surface you'd miss: a wishlist, abandoned-cart recovery email, admin-managed redirects (so old URLs keep their SEO), product CSV import/export, and a translation-management UI. Tax/VAT (Stripe Tax or built-in single-rate) and zone/weight shipping are in the box too — the things that are usually an app-chain on the incumbents. What is **not** in the box yet is a dedicated WooCommerce importer: products move by CSV (simple products), pages and posts by the URL import as drafts, old URLs by those admin-managed redirects — exact paths, one at a time. [Choose your path](/docs/getting-started/choose-your-path) works the migration through.

## When NOT to pick Cartwright

Do not pick Cartwright if you want zero ops. Shopify or another hosted tool is usually better when uptime, checkout, apps, tax, and hosting should be somebody else's problem.

Do not pick it if your team has no JavaScript or TypeScript capacity. The setup wizard removes some environment-file work, but production customization is still normal Next.js work.

Do not pick it if you need EDI, advanced B2B pricing, marketplace multi-tenancy, complex ERP sync, or multiple storefront tenants out of the box. Cartwright can be extended, but those are projects, not checkboxes.
